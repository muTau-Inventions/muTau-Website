services:
  llm:
    image: ollama/ollama:latest
    container_name: mutau-ollama 
    restart: always
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    command: ollama serve

volumes:
  ollama_data:
